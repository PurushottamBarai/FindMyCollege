{
  "name": "college-search-app",
  "version": "1.0.0",
  "description": "College search application with filters",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seedDataSimple.js",
    "clean": "node scripts/cleanDatabase.js",
    "reset": "npm run clean && npm run seed"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}